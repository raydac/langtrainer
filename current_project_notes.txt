## 1. Implementation Progress
- Bars module added: shared resource selector, draggable paired A/B bars with centered bold system-monospace LTR/RTL text and swap-on-drag ordering, 7-item stages with overlap, centered `NEXT` stage advance, completion banner, editor/resource module filtering via `BARS`.
- Common bundled lesson text normalized so `A`/`B` entries start with a capitalized cased character.
- Added Estonian-Russian external shopping lesson with 45 words and store phrases.
- Null handling cleanup: resource module lists now normalize to empty lists; several internal "not found" helpers use `Optional` instead of returning `null`.
- RTL support: JSON `rtl` list, Hebrew keyboard layout, module/editor/Bricks/Crossword behavior, expanded `en-he-small` sample.
- External lessons: OS default folder, `LANGTRAINER_EXTERNALS`, GitHub sync, hardened sync paths, dual `index.json` merge, source icons, opened files shared across compatible modules.
- Resource selectors: `Start` / `Open from file` / `Sync Lessons` layout, tooltips, uppercase folder labels, Start disabled until a row is selected.
- Virtual keyboard stays mouse-only to preserve typing focus; header row uses fixed control height.
- Bricks module has a `Show` help flash button beside the cue; brick text uses bold `LangTrainerFonts.SYSTEM_MONOSPACED`.
- Crossword: rectangular board scaling; translation hint stays centered; dialog tip re-centers after text-direction updates.
- Gson replaced with Jackson; bundled classpath UI icons; optional JetBrains Mono with system monospace fallback.

## 2. Architecture & Technical Discoveries
- `LangTrainerModuleId` drives Editor module checkboxes automatically; adding a module enum value exposes it in the resource editor.
- `DialogDefinition.modules()` is always non-null; an empty list means the resource is visible to all modules.
- `TextDirectionSupport` centralizes RTL orientation, alignment, and bidi embedding; RTL surfaces prefer `LangTrainerFonts.SYSTEM_MONOSPACED`.
- Text boundary cleanup uses `String.strip()` instead of `trim()` so Unicode whitespace is handled consistently.
- Crossword RTL: `placement.col()` anchors letter 0 (rightmost for horizontal RTL); re-apply `CENTER` on translation label after direction setup.
- Bricks RTL: mirrored build-strip margins; completion fly uses visible history width, not spacer width.
- External index merges `<externals>/common/jsons/index.json` and `<externals>/index.json`; entries may be absolute or relative to the index file.
- `GitHubFolderSynchronizer` uses anonymous GitHub API, rejects unsafe sync targets and non-portable paths; sync failures still show local resources when load succeeds.
- `InputEquivalenceSupport`: positional match when key/value sizes match, otherwise any-key; equal token sets act as equivalence classes.
- Build with `mvn22`; assembly descriptors need `**/*` includes; JSON via Jackson databind 2.21.

## 3. Technical Debt & Refactoring
- No `langtrainer-app` test sources yet; `InputEquivalenceSupport` is a good first unit-test target.

## 4. Current Work In Progress
- None.

## 5. Known Issues & Limitations
- None.

## 1. Implementation Progress (recent)
- Fly game backspace: do not re-normalize on document remove (Dialog parity); `mergeLettersIntoExpected` no longer appends trailing sentence punctuation after all letters are typed (punctuation stays overlay-only).

## 6. Open Questions & Pending Decisions
- None.

## 7. Useful References
- Compile app module with reactor context: `mvn22 -pl langtrainer-app -am compile`.
- Build app with reactor context: `mvn22 -pl langtrainer-app -am package`.
- Start app from Maven: `mvn22 -pl langtrainer-app -am exec:java`.

## 8. User Advice and Recommendations
- Keep module docs in `langtrainer-app/docs/Module_*.txt` synchronized when module behavior changes.
- Keep changelog entries compact and short; include only important user-facing information and avoid overloading the changelog with implementation details.
- Regularly review `current_project_notes.txt`, record note-review/cleanup actions, and compact or remove records that are outdated, repetitive, or no longer useful.
- Prefer avoiding direct `null` where practical; use empty collections for collection results and `Optional` for absent values when it improves functional, fluent handling.

## 9. Notes Maintenance Log
- 2026-05-26: Reviewed and compacted notes (~78 to ~40 lines); merged RTL/external-resource bullets; removed changelog/README/meta duplicates and resolved one-off bugfix history.
